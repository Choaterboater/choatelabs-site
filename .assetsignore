# Files Wrangler should NOT upload as static site assets

.git/
.git/**

.gitignore
.assetsignore

# Documentation, not for the live site
DEPLOY.md

# Wrangler internals (auto-generated during build)
.wrangler/
.wrangler/**
wrangler.jsonc
wrangler.toml
node_modules/
.cache/

# Worker source + leaderboard handlers (run as code, not served as files)
src/
src/**
functions/
functions/**
lumina-schema.sql

# macOS
.DS_Store
